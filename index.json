{
  "project": "AbsoluteHOTAS",
  "version": "3.0.0",
  "type": "Standalone SFSE Plugin",
  "summary": "Starfield Script Extender (SFSE) plugin providing direct HOTAS/HOSAS ship flight control via native DirectInput HID polling. Features an in-game ImGui binding wizard, per-axis inversion and sensitivity, 22 configurable ship action button bindings, digital axis emulation, and live config reload — no vJoy or Joystick Gremlin required.",
  "technologies": [
    "C++23",
    "Starfield Script Extender (SFSE)",
    "DirectInput 8",
    "Windows SendInput API",
    "ImGui (D3D12 overlay)",
    "MinHook",
    "x86-64 assembly & trampoline hooks"
  ],
  "directories": {
    "projects/AbsoluteHOTAS": {
      "path": "projects/AbsoluteHOTAS",
      "purpose": "Core plugin code including the main entry point, hooks, controller thread, and default configuration ini.",
      "subdirectories": {
        "src": "C++ source files implementing Main, low-level memory hooks, setting beacon, UI binding wizard, and DirectInput controller.",
        "include": "C++ header files containing class declarations and struct definitions.",
        "config": "Contains standalone AbsoluteHOTAS.ini configuration file."
      }
    },
    "shared": {
      "path": "shared",
      "purpose": "Common headers, global variables, and utilities shared within the project.",
      "subdirectories": {
        "src": "Global variable definitions, plugin info details, D3D12 UI hook, and runtime paths/logs configuration.",
        "include": "Common headers, precompiled header (PCH.h), DirectInput/KBM utilities, device manager, and global synchronization variables."
      }
    },
    "docs": {
      "path": "docs",
      "purpose": "Technical documentation containing flight control cluster memory maps, hardware specifications, and output scancode reference charts.",
      "subdirectories": {
        "reference": "Markdown reference files documenting key output scancodes, ini options, and control cluster architecture."
      }
    }
  },
  "files": {
    "projects/AbsoluteHOTAS/src/Main.cpp": {
      "role": "Plugin Loader & Entry Point",
      "purpose": "Defines SFSEPluginLoad which initializes log paths, sets up a vectored exception handler for crash monitoring, plants the Setting beacon, installs the low-level hooks, initializes the controller thread, and hooks D3D12 via UIHook."
    },
    "projects/AbsoluteHOTAS/src/ThrottleHook.cpp": {
      "role": "Low-level x86 Hook Manager",
      "purpose": "Handles AOB signature scanning inside the game's .text section, allocates 2GB-near memory trampolines, hooks the critical flight writer path, registers manual camera/rotational gates, and provides safe override logic for ship roll, pitch, yaw, and throttle (via per-offset silence gates)."
    },
    "projects/AbsoluteHOTAS/src/ThrottleController.cpp": {
      "role": "Orchestrator & DirectInput Polling Thread",
      "purpose": "Manages the execution thread of the DirectInput controller, normalizes and deadzones axis inputs, handles ship button bindings, and delegates processing to specific subsystems."
    },
    "projects/AbsoluteHOTAS/src/SignalHunter.cpp": {
      "role": "Memory Signature & Injection",
      "purpose": "Runs the 'Signal Hunter' algorithm to locate the dynamic control cluster (leveraging the fThrottleAtEngineStart beacon), performs throttle injection, manages reverse thrust/boost zones, and handles turn assist silence gates."
    },
    "projects/AbsoluteHOTAS/src/ShipOutput.cpp": {
      "role": "Ship Action & Output Management",
      "purpose": "Handles ref-counted holding state for specific ship buttons/actions, resolving overlapping commands and translating them to KBM injects."
    },
    "projects/AbsoluteHOTAS/src/AimController.cpp": {
      "role": "Aim & Source Object Reticle Injection",
      "purpose": "Drives the source-object reticle/cursor via DirectInput axes or buttons independently from steering, including HOSAM alignment assist logic."
    },
    "projects/AbsoluteHOTAS/src/SettingBeacon.cpp": {
      "role": "Bethesda Setting Scanner & Mod Beacon",
      "purpose": "Scans the game module's .data and .rdata memory sections to locate the fThrottleAtEngineStart and fRollDeadzone settings, planting a signpost value (0.0314f) and disabling the native roll deadzone."
    },
    "projects/AbsoluteHOTAS/src/BindingWizard.cpp": {
      "role": "ImGui Binding Wizard UI",
      "purpose": "Implements the interactive in-game overlay wizard allowing users to dynamically bind controller axes and buttons, calibrate deadzones, and configure sensitivities."
    },
    "projects/AbsoluteHOTAS/src/WizardCapture.cpp": {
      "role": "Binding Wizard Signal Capture",
      "purpose": "Handles live input capture for physical joystick buttons/axes within the ImGui wizard UI."
    },
    "projects/AbsoluteHOTAS/src/WizardConfig.cpp": {
      "role": "Binding Wizard Config Serializer",
      "purpose": "Serializes and saves custom controller configurations directly to AbsoluteHOTAS.ini from the ImGui wizard."
    },
    "projects/AbsoluteHOTAS/config/AbsoluteHOTAS.ini": {
      "role": "User Configuration File",
      "purpose": "Provides user-definable mapping settings for flight axes, gain parameters, physical button assignments, custom scancodes, and idle deadzones."
    },
    "shared/src/DeviceManager.cpp": {
      "role": "HID & DirectInput Device Discovery",
      "purpose": "Manages low-level DirectInput 8 device enumeration, initialization, and capability polling for connected HOTAS/HOSAS controllers."
    },
    "shared/src/UIHook.cpp": {
      "role": "D3D12 & ImGui Graphics Hook",
      "purpose": "Hooks the game's D3D12 Present and ResizeBuffers calls using MinHook to inject and render the ImGui controller configuration overlay."
    },
    "shared/src/RuntimePaths.cpp": {
      "role": "Path Resolver & Logger Utility",
      "purpose": "Resolves documents, SFSE plugins, and logs paths, handling thread-safe append operations to the plugin log file."
    }
  },
  "technical_details": {
    "flight_control_cluster": {
      "base_register": "RDI (captured from execution stream of the writer block)",
      "base_pointer_source": "R13 (upstream source object for processed intent)",
      "memory_lanes": {
        "cluster_base + 0x58": "Signed Roll / Lateral Output Lane (Float, -1.0 to +1.0)",
        "cluster_base + 0x5C": "Vertical Strafe Output Lane (Float)",
        "cluster_base + 0x60": "Signed Yaw Output Lane (Float, -1.0 to +1.0)",
        "cluster_base + 0x64": "Signed Pitch Output Lane (Float, -1.0 to +1.0)",
        "cluster_base + 0x68": "Signed Throttle Target / Reverse-Capable Throttle Lane (Float, 0.0 to 1.0 or signed)",
        "cluster_base + 0x6C": "Signed Throttle Companion / Current-State Lane (Float)"
      },
      "validation_heuristics": {
        "discovery_signal": "0.0314f (fThrottleAtEngineStart/fSignpostValue broadcasted on engine startup)",
        "corroboration": "Ensuring the read float values are finite, reasonable, and adjacent lanes scale together under isolated inputs."
      }
    },
    "zero_config_discovery": {
      "setting_beacon": "The plugin dynamically plants a float beacon (0.0314f) into the fThrottleAtEngineStart setting in memory. This eliminates the need for StarfieldCustom.ini edits and resolves addresses dynamically without Address Library."
    },
    "pilot_turn_assist": {
      "silence_gate_override": "Per-offset silence gates independently silence +0x68 (input target) and +0x6C (effective throttle). When pilot turn assist is active, the +0x6C gate is lifted, allowing the game's native rotation-deceleration assist to function, while the hardware input preserves target throttle on +0x68."
    },
    "shipping_paradigm": {
      "standalone_mode": "The mod compiles into AbsoluteHOTAS.dll and AbsoluteHOTAS.ini. Legacy scripting components (Papyrus, .esm, quest seat-trackers) have been removed from the release path, ensuring direct and standalone flight controller execution on DLL load."
    }
  },
  "task_context_mapping": {
    "how_to_add_new_ship_action": {
      "ini_config": "Add new i<Action>Button under [ShipButtons] and s<Action>Output under [ShipButtonOutputs] in projects/AbsoluteHOTAS/config/AbsoluteHOTAS.ini.",
      "controller_declaration": "Update ThrottleController::Config struct in projects/AbsoluteHOTAS/include/ThrottleController.h to include the new bindings.",
      "config_parsing": "Add standard BindingDef in projects/AbsoluteHOTAS/src/ThrottleController.cpp inside LoadShipButtonBindings to map the actionId, INI key, and output string key.",
      "default_output": "Register default action scancode in DefaultShipOutputForAction inside projects/AbsoluteHOTAS/src/ThrottleController.cpp.",
      "state_update": "Update state evaluation in UpdateShipButtonBindings to check physical press and trigger SetOutputHeld/PulseOutput."
    },
    "how_to_adjust_axis_normalization_or_reverse": {
      "formula_logic": "Modify ThrottleController::NormalizeAxis in projects/AbsoluteHOTAS/src/ThrottleController.cpp.",
      "variables": "Change configuration mappings in LoadConfig (detents, idle plateaus, reverse threshold) and adjust matching variables in include/ThrottleController.h."
    },
    "how_to_update_writer_block_hooks_for_game_patch": {
      "signature_update": "Verify assembly signature in InstallRotationalGates and raw Pattern structs in projects/AbsoluteHOTAS/src/ThrottleHook.cpp.",
      "call_rva_update": "Adjust version constant bounds and target displacement search bounds (e.g. 0x12B0000 to 0x12C0000 range check in ThrottleHook.cpp line 817)."
    }
  }
}
